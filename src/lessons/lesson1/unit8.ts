{
    function printSum(firstTerm: number, secondTerm: number) {
        console.log(firstTerm + secondTerm)
    }

    const firstNumber: number = 20
    const secondNumber: number = 30
    printSum(firstNumber, secondNumber)
}